# Issue #515: Fix: Fix: Fix: bounty protocol: public spec → add-only file → USDC to payTo

# Bounty Protocol: Public Spec → Add‑Only File → USDC to payTo → Paste TX Hash

**Version**: 1.0  
**Effective**: Immediate  
**Network**: Base `eip155:8453`  
**Asset**: USDC `0x833589fCD6eDb6E08f4c7C32D4f71b54bdA02913`  
**PayTo**: `0xAB745e5F576667037696e78ba7dA28E193E4423D`  
**Explorer**: https://basescan.org/address/0xAB745e5F576667037696e78ba7dA28E193E4423D

---

## Purpose

This protocol defines the canonical, non‑negotiable mechanism for delivering legal USDC to the live seller `payTo`, using this GitHub repository as the public specification and receipt book. It is **not** a token, **not** equity, and **not** a fundraising vehicle.

---

## Add‑Only Files Shipped This Session

These files are **immutable** for the duration of this session and must **not** be rewritten as part of child bounties:

- `docs/BOUNTIES.md`
- `.github/FUNDING.yml`
- `.github/ISSUE_TEMPLATE/paid-bounty.md`

---

## Sequence (Non‑Negotiable)

Every bounty execution **must** follow exactly one of these two sequences.

### Standard Order (Spec → Delivery → Payment → Hash)

1. **Public spec** lives on a `bounty:` issue (this document or a child issue referencing it).
2. **Operator** delivers a **new file only** on the `master` branch (no modifications to existing files except those explicitly permitted by the issue).
3. **Counterparty** sends USDC on Base to `0xAB745e5F576667037696e78ba7dA28E193E4423D` for the exact amount stated in the spec.
4. **Operator** pastes the on‑chain transaction hash as a comment on the issue within 1 hour of delivery.
5. **Verification** – any third party confirms the hash corresponds to a Base USDC transfer to the `payTo` address for the agreed amount.

### Short Form (Delivery → Payment → Hash)

Used only when the spec is already public and the amount is pre‑agreed.

1. Operator delivers the file.
2. Counterparty sends USDC to `payTo`.
3. Operator pastes the hash.

---

## Amount Rules

- All amounts are denominated in USDC (6 decimals).
- The exact USDC amount must be stated in the issue title or body before any work begins.
- No partial payments; the full amount must be sent in a single transaction.
- Gas fees are borne by the sender.

---

## Receipt Book

Every paid delivery is recorded by the transaction hash. The hash serves as the definitive proof of payment. The issue comment containing the hash is the official receipt.

---

## Enforcement

- If the operator delivers without a prior public spec, the bounty is void.
- If the counterparty sends USDC to any address other than `payTo`, the operator has no obligation to deliver.
- If the operator fails to paste the hash within the time limit, the counterparty may request a refund via the same `payTo` address (operator must sign a refund transaction upon request).
- Disputes are resolved by referencing the public issue and the on‑chain transaction history.

---

## Scope

This protocol applies to **all** bounties opened under this repository unless explicitly overridden by a child issue that references this document and provides a replacement `payTo` address. Any override must be approved by the repository maintainer and recorded as an add‑only amendment.

---

## Amendments

Amendments to this protocol are allowed only via a new pull request that adds a new version file (e.g., `PROTOCOL_v1.1.md`) while leaving this file unchanged. The new version must explicitly list the changes and maintain the add‑only principle for all shipped files.

---

## Final Step – Paste TX Hash

After the transfer is confirmed, the operator **must** paste the transaction hash in the bounty issue thread using the following format:

```
Payment TX: 0x[64-character hex hash]
Explorer: https://basescan.org/tx/0x[64-character hex hash]
```

This completes the protocol.

## Verification
- Generated by DevilX auto-claim (OpenRouter/NVIDIA)
- Thu Aug 27 12:01:52 UTC 2026

Closes #515
